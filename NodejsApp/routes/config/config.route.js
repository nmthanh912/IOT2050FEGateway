var express = require('express')
var router = express.Router()
var controller = require('../../controllers/config/config.controller')

// router.get('/', controller.getConfig)
// router.post('/', controller.postConfig)
// router.put('/:deviceId', controller.editConfig)
// router.delete('/:deviceId', controller.deleteConfig)

module.exports = router
