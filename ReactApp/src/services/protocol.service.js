import http from './http-common'

const protocolService = {
    get: () => {
        
    },
    add: data => {
        
    },
    modify: (id, data) => {
        
    },
    delete: id => {
        
    }
}

export default protocolService